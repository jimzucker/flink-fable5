Phase 8 — AWS Deployment & Load Tests (session 2026-08-01, Claude Code / Fable 5)
=================================================================================

PROMPT 8
--------
"what is needed to be able to deploy to aws" -> "creds done" -> (permission
granted for terraform apply) -> "run the load tests on aws"

RESPONSE (summary of the deployment)
------------------------------------
Prerequisites resolved interactively: AWS credentials (aws configure; account
760967184165, us-east-1) and Terraform (brew tap hashicorp/tap — Terraform
left Homebrew core after the license change).

terraform apply built the full stack: VPC + private subnets + NAT, MSK
Serverless (IAM auth), S3 (versioned jar), Managed Flink application
(FLINK-1_20, VPC-attached, OPERATOR-level CloudWatch metrics), ECS Fargate
generator via ECR, CloudWatch dashboard, least-privilege IAM.

Real-world deployment bumps, each found by verifying and each now captured
in the runbook:
1. JAVA VERSION: MSF's Flink 1.20 runtime is Java 11; the jar was Java 17
   bytecode -> "linkage failure" on start. Changed maven.compiler.release
   to 11... and hit bump #2:
2. STALE INCREMENTAL BUILD: mvn package without clean kept the Java 17
   classes (only the pom changed; sources looked fresh) — the "fixed" jar
   still failed identically. Verified bytecode version byte (61 vs 55)
   straight out of the jar; mvn clean package fixed it for real.
3. IMAGE ARCHITECTURE: docker build on Apple Silicon produced arm64; Fargate
   task wanted linux/amd64 -> CannotPullContainerError. Rebuilt with
   --platform linux/amd64.
4. AWS TAG-PROPAGATION RACE: destroy+recreate of the MSF app with the same
   name failed with ConcurrentModificationException while one retry
   succeeded server-side without Terraform knowing -> split-brain. Fixed
   with terraform import + reconcile apply.
5. zsh trivia: "$ECR:latest" invokes the :l history modifier — use
   "${ECR}:latest".

End state: app RUNNING (all operator chains up), generator in ECS steady
state producing against MSK, metrics flowing to CloudWatch.

AWS LOAD TESTS (scripts/aws_perf_probe.py — CloudWatch SEARCH queries,
rates scaled by parallelism after calibrating against the known baseline)

STEP 1 — BASELINE (10 trades/s, 20 prices/s, 5 accounts, parallelism 2):
  trades 10.0/s, prices 20.0/s, dedup out 9.6/s (5% dups), MV ~53/s
  (conflation active), busiest task 5 ms/s (0.5%), backpressure 0.
  Pipeline behavior identical to the laptop baseline.

STEP 2 — CASE 1 (1000 trades/s, tfvar-only): PASS.
  trades 1000.0/s sustained, dedup out 950.4/s (5% dups dropped),
  MV ~994/s (conflation holding), busiest task 21.5 ms/s (2.2% — vs 5.2%
  on the laptop), backpressure 0. 100x order rate, no performance impact,
  on 2 KPUs.

STEP 3 — STORM (1000 trades/s + 10,000 prices/s + 50 accounts, the exact
load that saturated the pre-conflation design locally): PASS.
  prices absorbed 10,002/s flat (full rate), trades 1002/s sustained,
  dedup 953/s, MV emissions 1,499/s (pre-conflation demand was 517k/s —
  345x less work), busiest task ~17%, backpressure 0, lag 0.
  2 KPUs absorb the full 500x price storm with the conflation window.

STEP 4 — CASE 2 (extreme price $10^13 at storm rates): PASS.
  trades 1000.0/s, prices 10,001/s flat, dedup 950.5/s, MV 1,500/s,
  busiest task ~13%, backpressure 0 — indistinguishable from the normal-
  price storm. All four AWS verdicts green: baseline parity, Case 1,
  storm, Case 2.

CLOSING: all four AWS verdicts green (baseline parity, Case 1, storm,
Case 2). Custom metrics fixed post-tests via DemoMetrics dual registration
(kinesisanalytics group) — all 8 demo* metrics now in CloudWatch (2.3M+
ticks conflated visible), dashboard fully populated. Stack left running at
storm load for demo; teardown = terraform destroy (~$1/hr while up).

DASHBOARD FIXES FOUND DURING THE RUN:
- CloudWatch dashboard widgets searched dimension "Operator"; MSF publishes
  "Task"/"TaskOperator" — fixed in dashboard.tf, applied live.
- MSF only exports custom user metrics registered under a special
  "kinesisanalytics" metric group — our plain Flink metrics (demo*) are
  visible to Prometheus locally but silently dropped by MSF. Code fix
  (dual registration) queued after the load tests so the app restart does
  not corrupt measurement windows. Observability-integration trap #3 of
  the day (after Prometheus name sanitization and the user_ scope).

FOLLOW-UP PROMPT
----------------
"lets add a new test 10x volume of trades adn 10x volume of prices at
highest prior rates" -> "how long are we goig to run we have tons of
backpressure, KPU max" -> "2" (destroy)

RESPONSE (summary)
Mega-test at 10,000 trades/s + 100,000 prices/s (generator parameterized to
2 vCPU, delivered the full 110k msgs/s): parse stage saturated at ~16.5k
msgs/s — the measured capacity of 2 KPUs. Conflation held (14.4M ticks
absorbed, MV bounded); bottleneck is linear-scaling JSON parse, so 110k/s
is a KPU dial (~P=14-16). Dialed load down immediately on the user's call,
recorded results, noted gotcha #8 (enable MSF snapshots in production —
without them any restart replays from earliest, so rescaling under backlog
multiplies work), and tore the stack down (terraform destroy) — test
complete, ~60-90M-record backlog made draining pointless. Total AWS phase:
all planned tests green, capacity knee measured, eight operational gotchas
documented.

FOLLOW-UP PROMPT
----------------
"did we scale the pipeline up to show we can scale to handle the volume?"
-> "yes" -> "can we rename the stack flink-fable5 to match the git repo and
start deploying it while the old one tears down" -> "we sould interate
faster" (+ status checks and "why are sinks red?")

RESPONSE (summary)
Honest gap identified: AWS rescale mechanism existed but linearity was
never measured on KPUs. Closed it: renamed stack to flink-fable5 (project
var default now matches the repo), deployed in a parallel Terraform
workspace while the old stack finished dissolving, enabled MSF snapshots
(gotcha #8 fix) and 16-partition topics so source parallelism scales past
4. Ran the ladder under sustained overload, iterating fast (saturation
confirmed from single samples, rungs chained back-to-back; the floor is
AWS itself: ~4 min per rescale + 1-min CloudWatch granularity):

  P=2, 2 KPUs, 35k offered  -> ~16.5k processed (saturated baseline)
  P=4, 4 KPUs, 35k offered  -> ~52k processed  (~2x)
  P=8, 8 KPUs, 70k offered  -> ~97k processed  (~2x again)

fullRestarts 0 across all rescales (snapshots working); one checkpoint
timeout under max backpressure, self-recovered. "Why are sinks red?" —
Flink UI colors operators by busy/backpressure; red across the graph
during deliberate overload is the pipeline at full effort, not a fault.
Verdict: volume is a KPU dial and the dial is measured — 110k msgs/s needs
~P=10. Generator dialed to baseline with P=8 kept for fast backlog drain.
